spring:
  application:
    name: GramSathi

  datasource:
    url: jdbc:postgresql://localhost:5432/gramsaathi
    username: postgres
    password: 258046

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true